"""
Agent Tools Registry — Tools available to the LangGraph agent.

Each tool is a callable that the agent can invoke during execution.
"""

from __future__ import annotations

from src.core.logger import get_logger

logger = get_logger(__name__)

# TODO: Register tools in Sprint 1, Day 5
# Example tools: rag_search, web_search, calculator
